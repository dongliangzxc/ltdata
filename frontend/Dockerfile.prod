FROM node:20-alpine AS builder

WORKDIR /app

COPY package*.json ./
RUN npm install --registry=https://registry.npmmirror.com

COPY . .
RUN npm run build

# ── 生产阶段：Nginx 托管静态文件 ──
FROM nginx:alpine

COPY --from=builder /app/dist /usr/share/nginx/html
# nginx 配置由 docker-compose 挂载进来

EXPOSE 80
