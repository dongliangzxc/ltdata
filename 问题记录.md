# 待确认问题记录

> 整理需求方未明确的问题，统一确认后更新。✅ = 已确认，⏳ = 待确认。

---

## 处理工作台

| # | 问题 | 结论 / 默认假设 | 状态 |
|---|------|----------------|------|
| 1 | 清洗任务进入处理后，“转移到其他任务”的目标任务选项默认按哪些条件过滤？ | 目标任务选项默认与当前任务的平台一致、月度一致；不默认限制为同品类 | ✅ |
| 2 | 清洗任务进入处理后，导出文件中品牌/型号相关表头如何展示？ | “品牌”表头改为“原品牌”，“品牌名称”表头改为“入库品牌”；“型号”和“型号名称”只保留“型号名称”的值，表头显示“型号” | ✅ |
| 3 | 清洗任务进入处理后，是否展示“已匹配 / 已确认条目”区域？ | 不展示该区域；移除页面中的整张“已匹配 / 已确认条目”表格和对应列表请求 | ✅ |
| 4 | 匹配结果页新增“平台、品牌、匹配型号、调整系数”筛选时，各筛选项按什么语义生效？ | 平台按 raw_data.platform 精确筛选；品牌按原品牌/入库品牌模糊筛选；匹配型号按型号代码/型号名称模糊筛选；调整系数按有/无 sales_coefficient 筛选 | ✅ |
| 5 | 匹配结果页“原价格”全为空是否符合预期？ | 已确认不是预期；根因为匹配结果列表接口构造返回值时漏传 raw_data.price，已改为返回 price=rd.price | ✅ |
| 6 | 匹配结果页整体计算按当前页还是当前筛选全量结果统计？ | 按当前查询条件下的全部匹配结果统计，受任务、Tab、平台、品牌、型号、价格预警、调整系数、关键词筛选影响，不受分页影响 | ✅ |
| 7 | 匹配结果页整体计算各字段按什么口径计算？ | 按查出结果列合计：原价格 = SUM(原价格)，调整后价格 = SUM(调整后价格)，原销量 = SUM(原销量)，调整后销量 = SUM(调整后销量)，整体消费价格 = SUM(原价格 × 原销量)，调整后消费价格 = SUM(调整后价格 × 调整后销量) | ✅ |

---
| 4 | 清洗任务进入处理后，“品类字段要求”新增能力具体新增什么？ | 不是新增全局品类；新增的是当前品类下的一条品类字段要求记录 | ✅ |
| 5 | “品类字段要求”是否支持搜索已有的记录？ | 支持在当前品类下搜索已有的品类字段要求记录 | ✅ |
| 6 | “品类字段要求”与“产品字段定义”如何联动修改？ | 默认假设：新增或搜索品类字段要求记录时，同步更新对应产品字段定义 | ⏳ 待确认 |
| 7 | “处理工作台”权限是否需要增加品类范围控制？ | 已确认：需要参考数据管理中的品类权限处理，为处理工作台增加品类相关权限控制 | ✅ |
| 8 | 处理工作台中哪些页面/API 需要套用品类权限范围？ | 已确认：优先覆盖带有 `category_code` 或品类筛选的数据页面/API；清洗任务也按品类权限处理；品牌、型号、URL 映射等不直接按品类归属的数据页面暂不纳入 | ✅ |
| 9 | 没有配置 `category_permissions` 的用户，在处理工作台里如何看待品类范围？ | 已确认：与数据管理一致，`category_permissions` 为空时看全部品类 | ✅ |

## 服务器迁移

| # | 问题 | 结论 / 默认假设 | 状态 |
|---|------|----------------|------|
| 1 | 旧服务器数据迁移到新服务器时是否允许短暂停机？ | 允许短暂停机，优先采用最终停机导出数据库并同步持久化文件，保证数据一致性 | ✅ |

---

## 五期：价格审核 + 销量调整

| # | 问题 | 结论 / 默认假设 | 状态 |
|---|------|----------------|------|
| 1 | 价格审核的“过去 6 个月历史均价”按哪个维度计算？ | 按型号维度计算：同一 model_id/model_code 的 published_items.price 历史均价 | ✅ |
| 2 | 销量调整系数与已有修正规则如何叠加？ | 先应用已有修正规则，再乘单条 sales_coefficient，单条人工调整作为最后兜底 | ✅ |
| 3 | 价格异常是否阻断发布？ | 只提示不阻断发布；high/low/no_history 在匹配确认页展示预警，发布仍可继续 | ✅ |
| 4 | 五期采用独立审核页还是集成到现有匹配确认页？ | 采用独立页面方案：在处理工作台中新增“数据调整”，放在“清洗任务”后、“匹配结果”前；清洗任务完成后发布到分析库前的数据放在“数据调整”页处理 | ✅ |
| 5 | 匹配确认页表格中商品 URL 入口放在哪里？ | URL 放在“操作”列内，紧跟“重新选择”后面，有链接时可点击打开 | ✅ |
| 6 | 匹配结果独立页与匹配确认页的已匹配/已确认表格字段以哪套为准？ | 采用新共享列：商品名称、入库品牌、匹配型号、价格预警、原价格、现价格、原销量、调整系数、调整后销量、操作；操作列内展示“重新选择 / URL”，两个页面复用同一列定义 | ✅ |
| 7 | “转移到其他清洗任务”目标选择是否采用品类/平台/月度筛选后点选任务，而不是三段选择后自动提交？ | 已确认采用筛选后点选任务：品类、平台、月度作为筛选条件缩小任务列表，最终仍需用户明确点选目标任务并确认转移 | ✅ |
| 8 | 转移弹窗查不到 110/62 这类页面可见任务时，目标任务筛选应按清洗任务新字段，还是兼容旧清洗任务/分发批次展示口径？ | 兼容旧清洗任务元数据的后端搜索方案已回退；需重新确认目标选择口径，避免旧任务被错误纳入或筛选结果更混乱 | ⏳ 待确认 |
| 9 | 清洗任务完成后发布到分析库的数据是否继续放在清洗任务详情下，还是新增独立入口？ | 已最新确认：页面名称改回“匹配结果”，主入口为 `/match-results`；`/data-adjustment` 仅保留兼容重定向；清洗任务页继续使用 `previewCleanJob(clean_job_id)` 弹窗预览 | ✅ |

---

## 六期：数据看板 + 灵活导出

| # | 问题 | 结论 / 默认假设 | 状态 |
|---|------|----------------|------|
| 1 | 第六期数据看板优先服务哪种使用场景？ | 优先运营分析视角：筛选后看明细聚合表、品牌/型号/平台排行，偏可查可导出 | ✅ |
| 2 | 运营看板主表默认按什么粒度聚合？ | 支持切换聚合粒度：品牌 / 型号 / 平台 / 品类，默认型号 | ✅ |
| 3 | 第六期灵活导出应该导出哪类数据？ | 聚合结果和当前筛选下的发布明细都要支持导出；明细导出支持字段勾选 | ✅ |
| 4 | 看板里的销量指标默认使用哪个口径？ | 原始销量和修正后销量都展示，默认排序使用修正后销量；修正后销量为空时回退原始销量 | ✅ |
| 5 | 销额和均价是否也要做双口径？ | 销量双口径；销额和均价第六期暂不做双口径，继续使用原始销额/原始均价，避免五期只调销量不调销售额导致口径混淆 | ✅ |
| 6 | 第六期是否要实现同比指标？ | 暂不做同比；先把筛选、聚合、排行、导出做好 | ✅ |
| 7 | 第六期看板筛选条件先做哪些？ | 年月、品牌、品类、平台、型号关键词、商品关键词全都要支持 | ✅ |
| 8 | 明细导出的字段勾选是否需要记住上次选择？ | 暂不记忆偏好；导出弹窗默认全选，用户临时取消勾选即可 | ✅ |
| 9 | 第六期采用哪种实现方案？ | 采用方案 1：运营分析表优先、轻图表；先做筛选区、指标卡、可切换聚合表和两类导出 | ✅ |
| 10 | 第六期后端 API 和指标口径如何设计？ | 新增 /api/analytics 独立路由；提供 filters、summary、summary export、detail export；修正后销量用 COALESCE(corrected_sales_qty, sales_qty)，均价用原始销额 / 原始销量 | ✅ |
| 11 | 第六期前端页面交互如何设计？ | 新增 /dashboard 数据看板；筛选后点击查询刷新；聚合粒度可切换；默认按修正后销量倒序；明细导出弹窗默认全选字段 | ✅ |
| 12 | 分发结果下载记录删除时是否同步删除已生成导出文件？ | 删除列表记录时同步删除服务器上的已生成导出文件和下载 token，避免旧链接继续下载 | ✅ |
| 13 | 分发结果下载记录超过首屏 50 条后如何查看更早记录？ | 已确认：导出任务列表需要分页展示，不能只固定请求前 50 条；用户应能翻页查看更早下载记录 | ✅ |
| 14 | 数据导出页改为按月度、品类、平台导出时，导出来源应使用已发布/看板明细数据还是匹配结果数据？ | 已确认：导出内容和之前一致，继续导出匹配结果 Excel，只是筛选规则从清洗任务改为月度（可多选）、品类、平台（可多选） | ✅ |

---

## 历史库匹配

| # | 问题 | 结论 / 默认假设 | 状态 |
|---|------|----------------|------|
| 1 | 历史原数据的 Excel 列结构是否与现有上传 Excel 一致（平台/商品名/价格/数量等）？ | 历史库按“历史原始明细 + 确认型号结果在同一张 Excel 表”处理，每行视为一条线下已确认的工作台结果 | ✅ |
| 2 | 历史正确数据库（确认对照表）的 Excel 具体有哪些列？ | 当前样例列：年、月、周、报告类型、渠道、商场、品类、品牌、型号、品类码、品牌码、型号码、标题、销额、销量、单价、网址；其中「型号」视为确认型号，但历史线下不完全规范，可能是型号编码或型号名称，导入时先按 model_code 匹配，再按 model_name 唯一命中匹配 | ✅ |
| 3 | 历史原数据与历史对照表通过什么字段关联？ | 第一版优先按 URL 解析出的平台+商品ID、平台+网址原文、平台+商场+标题归一化完全一致做高置信匹配；样例无显式商品ID列 | ✅ |
| 4 | 历史原数据 Excel 是否包含月份字段？格式是什么？ | 包含独立「年」「月」「周」列；导入时组合为 month=YYYY-MM，并保留 week | ✅ |
| 5 | 历史对照表会有多份吗？如有，同一商品在不同批次型号不一致时以哪次为准？ | 旧历史库数据当前无有效存量，可直接废弃不迁移；新历史库按“历史确认结果导入”重建，重复判定需包含数据周期（如 year/month/week 或 month），同一商品不同月份可同时存在，同一周期内重复高置信匹配键以最新导入批次为准 | ✅ |
| 6 | 匹配汇总页面是否需要区分「历史库命中」与「文本匹配」的数量统计？ | 当前实现：历史库命中计入 `matched` 汇总桶，`来源` 列可按条查看，但无专项聚合计数 | ⏳ 待需求方确认是否需要新增计数器 |
| 7 | 历史库匹配新数据时是否允许商品名称模糊相似匹配？ | 第一版只做高置信精确匹配：platform+item_id、URL 解析 item_id、platform+item_url、platform+shop_name+item_name 归一化完全一致；暂不做模糊相似度 | ✅ |
| 8 | 历史库是否允许型号为空的记录导入？ | 允许。型号为空表示历史库里已有商品 URL/品牌等商品线索但型号尚未补全，后续可继续补充；这类记录仍属于历史库，链接是关键匹配依据 | ✅ |
| 9 | 历史库是否需要兼容线下历史数据库的多种旧表头格式？ | 需要。后续历史库导入应支持笔记本、路由器、门锁等历史明细库直接上传，通过列别名和明细 sheet 自动识别转换为标准历史库列 | ✅ |
| 10 | 历史库兼容旧格式导入时是否需要先向用户反馈映射结果？ | 需要。导入流程应类似原始数据上传：先解析 sheet/列映射/年月品类推断并展示预览，用户确认后再正式入库 | ✅ |
| 11 | 历史库 Excel 中的品类属性列如何处理？ | 属性列属于成品字段定义/成品属性管理处理的型号属性体系，不作为历史库映射表的标准字段；导入历史库时只沉淀商品历史明细和确认型号，属性可复用现有成品属性管理导入能力一次性处理 | ✅ |
| 12 | 历史库导入时型号文本找不到现有型号库怎么办？ | 自动创建型号记录；属性仍由产品字段定义/产品属性管理后续维护 | ✅ |
| 13 | 导入三份历史库前是否需要先补齐产品字段定义和产品属性管理？ | 需要。三份历史库属于一次性历史初始化：先从 Excel 中提取各品类属性列和型号属性值，一次性填入产品字段定义/产品属性管理；后续维护主要在平台页面操作，不需要为历史属性导入单独做长期入口 | ✅ |
| 14 | 历史属性初始化遇到品牌或型号为空/为 `-` 的记录如何处理？ | 品牌有效但型号为空或 `-` 时允许保留为待补型号记录，后续再补型号；品牌和型号都为空或 `-` 时不可用，不写入成品属性管理 | ✅ |
| 15 | 匹配确认页是否展示未补规格属性？ | 暂不在匹配确认页待处理区展示未补属性；规格属性缺失不参与当前匹配分流 | ✅ |
| 16 | URL 映射库或历史库链接命中的结果如何命名？ | 统一展示为“精准匹配”，表示商品链接在 URL 映射库或历史库已有命中 | ✅ |
| 17 | 匹配来源是否直接展示 S1/S2/S3/S4 等内部阶段名？ | 不展示内部阶段名，改为业务可读来源文案，如 URL映射命中、历史库命中、品牌字段匹配等 | ✅ |
| 18 | 品牌码或型号码为 `-` / `--` / 空的型号能否作为自动匹配结果？ | 不能作为有效自动匹配结果；页面展示时也不把 `[-] -` 视作有效型号 | ✅ |
| 18.1 | 匹配确认页哪些待处理列表需要展示商品链接？ | 待确认、未识别品牌、URL映射待确认都需要展示商品链接，方便分析师打开商品判断归属 | ✅ |
| 19 | 历史库数据是否需要进入分析库？ | 需要。历史库不仅是匹配参考，也是历史分析数据来源；首次录入平台前的历史数据需要能进入分析库，否则平台缺少既有经营数据 | ✅ |
| 20 | 新数据人工确认后是否沉淀 URL 映射库？ | 需要。URL 映射库是长期沉淀的纯粹匹配库，不只是临时缓存；新数据确认型号后应写入 URL 映射库，供后续同链接精准匹配 | ✅ |
| 21 | 历史库和 URL 映射库的职责如何区分？ | 历史库承担历史数据导入和历史发布，允许信息不完整；URL 映射库承担持续沉淀的链接级匹配关系，尽量保持结构纯粹、面向后续自动匹配 | ✅ |
| 22.1 | 映射管理里仍可见无权限耳机数据时，是否按历史库映射管理统一套用品类权限？ | 已确认：历史库也按照品类权限处理；历史库映射列表、批次列表和删除操作都应按当前用户可见品类过滤/拦截 | ✅ |

| 22 | 匹配确认页“一键确认”选择型号能力应采用哪种粒度？ | 已确认：一键确认前在确认弹窗中统一选择一个目标型号，本次选中的所有记录都确认成该型号，不再直接确认为候选型号；型号选择能力需与单次确认一致，支持搜索选择和新建型号后回填选择；候选型号只作为分布提示，不再决定批量确认目标 | ✅ |

---

## 待处理工作台

| # | 问题 | 结论 / 默认假设 | 状态 |
|---|------|----------------|------|
| 1 | 有争议、未匹配、URL待确认、未识别品牌数据应如何给分析师处理？ | 第一期按“任务内处理”设计，升级现有匹配确认页为左侧队列 + 右侧详情处理面板，不做个人队列 | ✅ |
| 2 | 待处理工作台需要哪些处理动作？ | 支持确认当前候选、选择其他型号确认、排除、暂存争议；确认后沉淀 URL 映射库，排除/争议不发布且不写 URL 映射库 | ✅ |
| 3 | 是否需要新增争议状态？ | 新增 `disputed` 状态表示暂存争议；发布只发布 `url_matched` / `matched` / `confirmed` | ✅ |
| 4 | 是否需要记录操作人？ | 第一期先记录 `reviewed_at`、`review_note`、`dispute_reason`；操作人/负责人留待个人队列阶段再补 | ✅ |
| 5 | 详情处理里“相同 case”批量确认按什么口径？ | 按商品名称归一化后完全一致判断；执行前必须预览命中条数和受影响记录，避免误批量 | ✅ |
| 6 | 清洗任务详情增强采用什么整体方向？ | 在现有左侧队列 + 右侧详情处理工作台中增强批量确认、属性提示、干扰项规则浮窗和重清洗闭环，不新拆独立页面 | ✅ |
| 7 | 新增干扰项规则后重清洗是否会让已人工确认 URL 的数据重新回到待处理？ | 不应重复让分析师处理；已确认写入 URL 映射库后，重清洗/重匹配会优先通过 URL 映射命中，回到可发布状态而不是待处理 | ✅ |
| 8 | 右侧复核工作台是否只服务待处理/争议数据？ | 不应只看待处理；右侧详情工作台应覆盖待处理、争议、已匹配、已确认，并补充干扰项过滤记录 Tab，方便确认后继续查看属性和完整复核链路 | ✅ |
| 9 | 运动相机复核详情中显示智能门锁属性时，应以清洗任务品类还是当前匹配型号品类作为属性展示来源？ | 当前实现优先使用当前匹配型号的 category_code 查询字段定义；如型号被归到智能门锁，会显示智能门锁属性。默认先排查型号品类归属和字段定义数据，不直接改逻辑 | ⏳ 待确认 |
| 10 | 复核详情里的候选型号和“选择其他型号确认”搜索范围应按什么品类限定？ | 按当前清洗任务品类限定，不做全量型号搜索；避免运动相机任务出现智能门锁等其他品类型号候选 | ✅ |
| 11 | 已匹配 / 已确认条目确认错品牌或型号后，需要支持哪种纠错方式？ | 采用单条纠错：在已匹配 / 已确认条目中对单行重新选择型号，确认后更新该条结果 | ✅ |
| 12 | 单条纠错确认新型号后，是否同步更新商品链接 URL 映射库？ | 同步覆盖 URL 映射库；纠错视为最终人工结论，避免后续同链接重新自动匹配回旧型号 | ✅ |
| 13 | 已匹配 / 已确认条目的单条纠错入口采用哪种交互方案？ | 采用方案 1：在底部表格增加“重新选择”操作，打开弹窗完成单条型号纠错 | ✅ |
| 13 | 干扰项规则点击“修改”时是否应与快速新增表单字段一致？ | 已确认：修改表单升级为与快速新增一致，支持品牌、商品名称包含/不包含、启用参考价格条件、价格关系、价格字段，并回填已有规则值 | ✅ |
| 14 | 干扰项规则的品牌条件应匹配上传原始品牌还是入库标准品牌？ | 已确认：品牌条件应同时支持上传原始品牌和品牌别名标准化后的入库品牌，避免页面显示“入库品牌”命中但规则不生效 | ✅ |
| 15 | 一键批量确认覆盖哪些 Tab？ | 仅 URL映射待确认 / 待确认 生效；争议复核等其他 Tab 不做 | ✅ |
| 16 | 一键确认的无效候选如何处理？ | 已更新：一键确认前由用户统一选择目标型号，候选型号只作为分布提示，品牌码/型号码为空或 `-` / `--` 不再阻止批量确认；未识别品牌仍禁用，价格预警不算无效 | ✅ |
| 17 | 跨页全选如何做？ | 支持"全选当前页"和"选择全部搜索结果"两级；筛选条件（关键字/Tab/品类/排序）变化即清空 | ✅ |
| 18 | 批量接口上限？ | `ids` 模式 200 条、`filter` 模式 500 条；超上限按 sort_by 截断到前 500，`truncated=true` | ✅ |
| 19 | URL 映射冲突如何处理？ | 归为失败，不覆盖历史沉淀；失败原因文案：`URL 映射冲突：指向 [<brand>] <model>` | ✅ |

---

## 原始数据

| # | 问题 | 结论 / 默认假设 | 状态 |
|---|------|----------------|------|
| 1 | 原始数据页价格筛选的区间边界如何处理？ | 按闭区间处理：最低价 `>= price_min`、最高价 `<= price_max`；前端只保留最低价、最高价两个输入框，不做快捷区间选项 | ✅ |
| 2 | 原始数据页价格筛选是否影响统计和导出？ | 列表、统计、导出共用同一套筛选条件处理 | ✅ |
| 3 | 原始数据页月份筛选是否支持多选？ | 支持多月份查询；列表、统计、导出都按所选多个月份过滤 | ✅ |
| 4 | 原始数据页是否保留文件筛选？ | 暂时隐藏「选择文件」筛选项，保留其他筛选条件 | ✅ |

---

## 查询工作台

| # | 问题 | 结论 / 默认假设 | 状态 |
|---|------|----------------|------|
| 1 | 查询工作台顶部筛选项保留哪些？ | 筛选项按年度、月度、品类、平台、品牌、型号、网址展示，并保留「搜索宝贝名称」 | ✅ |
| 2 | 查询工作台结果表格列如何展示？ | 列顺序为序号、年度、月度、品类、平台、商品名称、网址、图片地址、原品牌、型号、型号别名、判断类型、销量、平均价格、操作人、操作时间 | ✅ |
| 3 | 查询工作台“操作人”取哪个业务动作的人？ | 暂无真实落库字段，当前先展示占位 `-`；待确认后可补为匹配确认人、发布人或最后编辑人 | ⏳ 待确认 |

---

## 前端页面布局调整

| # | 问题 | 结论 / 默认假设 | 状态 |
|---|------|----------------|------|
| 1 | 映射管理主筛选区是否保留导入年份、导入月份？ | 先删除主筛选区里的「导入年份」「导入月份」，保留「导入 Excel」按钮；如后续需要导入批次年月，再放入导入弹窗 | ✅ |
| 2 | 产品字段定义和产品属性管理上传前是否提供下载模板？ | 采用后端下载接口：产品字段定义下载后端包内模板「洛图科技—产品段属性说明-模板.xlsx」，产品属性管理动态生成「型号」「型号规格」两 Sheet 模板；前端在导入按钮旁提供下载模板按钮，并打通当前导入流程以读取「型号规格」Sheet，下载模板上传时自动套用内置字段映射 | ✅ |
| 3 | 产品字段定义模板 sheet 名如何识别品类？ | sheet 名可以是已有品类码或品类名称；导入时统一匹配为品类码，匹配不到则报错提示，不允许把未知 sheet 名直接写入品类码 | ✅ |
| 4 | 产品字段定义页品类筛选是否支持输入搜索？ | 支持；筛选下拉可按品类码或品类名称输入搜索 | ✅ |
| 5 | 任务复核工作台右侧详情面板中，信息展示区与热编辑区如何调整？ | 按截图顺序展示：商品详情信息放在“详情处理”标题下方，系统来源和 URL 线索不展示；当前型号区域同时保留搜索/选择其他型号确认和新建型号入口；随后依次展示候选型号、相同 case 批量处理、品类属性 / 当前型号规格 / 本条自动补充属性 | ✅ |

---

## 产品字段定义 / 产品属性管理

| # | 问题 | 结论 / 默认假设 | 状态 |
|---|------|----------------|------|
| 1 | “价格段”属性是否允许人工录入任意值？ | 默认不允许人工任意录入；价格段作为特殊系统属性，由品类配置分段规则后按商品价格自动计算，例如 `<100`、`100~599`、`600~999`、`>1000` | ⏳ 待确认 |
| 2 | 删除产品属性/型号时，已沉淀的历史库和 URL 映射库记录如何处理？ | 默认保留历史库和 URL 映射库记录，只清空其 `model_id` 引用；仍被匹配规则引用的型号需先删除或调整规则 | ⏳ 待确认 |
| 3 | 产品属性管理 Excel 导入时「型号规格」sheet 没有进库怎么排查？ | 已修复三点：(a) 规格 sheet 的 `品牌码/型号码` 与型号 sheet 一致做 strip，避免空格差异导致匹配失败被静默丢弃；(b) 规格行找不到对应型号时写入 `errors` 让用户可见；(c) 导入结果弹窗展示 `写入规格 N 条`；此外规格处理不再依赖“型号 sheet 至少入库一条”，库中已存在的型号也允许只更新规格 | ✅ |

---

## 品牌管理页

| # | 问题 | 结论 / 默认假设 | 状态 |
|---|------|----------------|------|
| 1 | 品牌管理表格需要新增哪些列？ | 新增「上传时品牌名称」「修改后名称」「品类」三列，其余保留品牌码 / 型号数 / 写法别名数 | ✅ |
| 2 | 「上传时品牌名称」具体存什么？ | 首次创建品牌时（手动新建或型号库导入自动补建）录入的 brand_name，一次写入 brands.original_brand_name 后不再改动 | ✅ |
| 3 | 「修改后名称」是什么字段？ | 复用现有 brands.brand_name；后续允许在品牌管理页编辑该字段，作为可维护的规范名 | ✅ |
| 4 | 存量 brands 记录如何回填 original_brand_name？ | 采用创建时初值锁定：迁移执行时把当前 brand_name 直接回填 original_brand_name，视为该品牌创建时的原始名 | ✅ |
| 5 | 品类列如何展示？ | 新增独立品类列；一个品牌下型号跨品类时全部以 Tag 列出，按品类码升序 | ✅ |
| 6 | 品牌管理页搜索框搜索哪些字段？ | 搜索范围限定为品牌码、上传时品牌名称、修改后名称；不搜索品类和写法别名 | ✅ |
| 7 | 品牌管理页修改功能允许改哪些字段？ | 只允许修改「修改后名称」即 brands.brand_name；不允许修改品牌码和上传时品牌名称 | ✅ |
| 8 | 品牌管理页品类查询支持单选还是多选？ | 使用单选品类下拉，支持搜索和清空；与关键词搜索组合过滤 | ✅ |
| 9 | 修改品牌别名时，如果目标别名已被其他品牌占用怎么办？ | 直接阻止保存并提示别名已存在，不允许迁移到当前品牌 | ✅ |
| 10 | 品牌别名编辑入口采用哪种交互方案？ | 采用方案 1：在展开行的别名列表里为单条别名提供“编辑”按钮，修改仅影响该别名记录 | ✅ |
| 11 | 品牌管理页顶部筛选区如何调整避免品类下拉单独换行？ | 采用标题与「新建品牌」同一行、搜索框与品类下拉同一筛选行的布局；桌面端筛选控件并排展示 | ✅ |

---

## 内联创建品牌与型号

| # | 问题 | 结论 / 默认假设 | 状态 |
|---|------|----------------|------|
| 1 | 产品属性管理导入遇到 brands 表中不存在的品牌码时，是否自动创建品牌主数据？ | 默认假设：为保持现有批量导入流程不中断，导入接口自动补 brands；普通 POST /api/models 仍要求先选择或创建品牌 | ⏳ 待确认 |
| 2 | 新建型号弹窗的规格属性区域如何布局？ | 规格属性区放在「品类」之后（在上市信息之前），按“必填 / 选填”分组展示；必填项加校验，选填项允许空值 | ✅ |

---

## 用户管理 / 权限管理

| # | 问题 | 结论 / 默认假设 | 状态 |
|---|------|----------------|------|
| 1 | 用户权限是否可以顺手调整现有菜单目录归属？ | 不允许调整现有目录归属；权限必须严格按当前已确定目录包住：数据管理、处理工作台、成品管理，仅允许新增用户管理目录 | ✅ |
| 2 | 用户管理列表线上仍出现“目录权限”和“最后登录”列内容重叠时，是否按重新提交并部署前端修复处理？ | 已确认线上前端包包含布局修复；当前缺口不是部署未生效，而是尚未实现“品类级权限”需求 | ✅ |

---

## 全局时间展示

| # | 问题 | 结论 / 默认假设 | 状态 |
|---|------|----------------|------|
| 1 | 平台内 created_at / updated_at / uploaded_at / finished_at 等用户可见时间按什么时区展示？ | 后端统一按北京时间字符串返回，前端直接展示或截取日期部分，不再用浏览器时区二次解析 | ✅ |

---

## 数据上传

| # | 问题 | 结论 / 默认假设 | 状态 |
|---|------|----------------|------|
| 1 | 是否保留上传确认/列映射步骤？ | 保留上传确认步骤，用户仍需确认列映射后再入库；不额外增加前 N 行字段格式预检查 | ✅ |
| 2 | 上传稳定性优先采用哪种方案？ | 采用方案 A：先做持久化上传任务、分阶段进度和断线后继续查看；暂不引入外部任务队列，不在本阶段重写 Excel 流式解析 | ✅ |
| 3 | 上传处理任务列表应该显示哪些任务？ | 仅显示已拿到文件名且仍在处理或失败的任务；已完成任务归入上传历史，不在任务列表长期展示 | ✅ |
| 4 | 上传处理任务是否需要取消、恢复和提速能力？ | 先做取消按钮、自动识别服务重启造成的中断任务、去重/写入批次提速；暂不做继续处理/恢复 | ✅ |
| 5 | 上传历史的上传时间按什么时区展示？ | 后端统一按北京时间字符串返回，前端直接展示，不再按浏览器时区二次解析 | ✅ |
| 6 | 数据上传页是否保留上传月份维度？ | 删除上传月份选择、上传历史月份筛选和历史表格月份列；文件自身月份范围仍保留 | ✅ |
| 7 | 上传历史长文件名如何查看完整内容？ | 上传历史表格支持横向滚动，文件名列加宽并关闭省略，用户可拖动横向滚动查看完整文件名 | ✅ |
| 8 | 上传处理任务失败后是否需要清理入口？ | 失败/已取消的上传处理任务支持删除，运行中和等待中任务需先取消或结束后再删除 | ✅ |
| 9 | 上传历史列表的“下载”应下载哪份文件？ | 下载用户当初上传的原始文件，文件名保持原上传文件名；若文件已不存在则提示不可下载 | ✅ |
| 10 | 上传历史原始文件下载接口较慢时如何反馈进度？ | 改为后台下载准备任务，前端展示进度并每 15 秒静默刷新任务状态；任务完成后再提供实际下载入口 | ✅ |

---

## 数据分发

| # | 问题 | 结论 / 默认假设 | 状态 |
|---|------|----------------|------|
| 1 | 分发明细中“未识别”数据是否需要可查看具体原始行？ | 在分发明细中显示“未识别 X 条”，点击打开分页明细弹窗查看未命中 raw_data 原始商品行 | ✅ |
| 2 | 分发规则命中统计是否需要支持直接编辑对应规则？ | 支持点击规则打开编辑面板并保存规则；保存后只更新规则配置，不自动改已完成批次，提示需要重新分发后生效 | ✅ |
| 3 | 分发结果增强采用哪种页面方案？ | 采用方案 A：增强现有“查看明细”弹窗，分区展示汇总、品类统计、规则命中统计，并通过二级弹窗查看未识别明细 | ✅ |
| 4 | 分发明细弹窗新增“单独分发入口”时，分发粒度是按品类，还是按品类 + 平台？ | 按当前明细行的品类/选项加入清洗任务的待入清洗队列；该操作不重新生成分发批次，不改变数据分发页当前列表、状态或查看明细内容 | ✅ |
| 4 | 分发结果下载的月份多选接口是否需要兼容旧的单月参数？ | 新增 months 数组支持多选，同时保留 month 单值入参兼容旧调用 | ✅ |
| 4 | 分发规则筛选后总数量显示在哪里？ | 放在规则表格下方左侧，显示当前筛选结果总数 | ✅ |
| 5 | 原始数据命中多条分发规则时如何处理？ | 分发不是互斥分类；只要命中规则就应分发到对应类目，同一条 raw_data 可以进入多个类目 | ✅ |
| 6 | 同一条 raw_data 命中同一类目的多条分发规则时是否去重？ | 同一 batch + raw_data + category_code 只保留一条，matched_rule_id 记录优先级最高的命中规则 | ✅ |
| 7 | 分发页追加清洗入口时，“全量清洗入库”按什么粒度执行？ | 采用“按类目逐个清洗”的方案：全量入口对该分发批次下每个类目分别创建清洗任务，避免把不同类目混在一个清洗上下文里 | ✅ |
| 8 | 单个类目清洗入口放在哪里？ | 原方案放在分发明细弹窗的类目统计表操作列；现反馈该入口不符合分析师工作流，需调整为分发结束后由分析师在清洗任务页按品类/平台维护任务 | ⏳ 待重新确认 |
| 9 | 分发后的清洗逻辑口径是什么？ | 清洗不是重新分发，而是在当前类目范围内根据干预库排除不符合类目需求的数据，并做必要的标准化处理 | ✅ |
| 10 | 分发后生成的清洗任务如何让分析师识别自己负责的内容？ | 清洗任务需要明显展示来源分发批次、品类/类目、负责人或可归属信息；否则多个分析师无法区分哪些任务属于自己负责的品类 | ✅ |
| 11 | 分发页是否应该承担创建清洗任务？ | 不承担：分发完成后只进入品类/平台待处理池；分析师在清洗任务页按自己负责的品类/平台创建或更新任务 | ✅ |
| 12 | 同一品类/平台已有清洗任务时，新分发数据如何进入任务？ | 采用按周期增量更新：同一品类+平台+月份/周期已有任务时，新分发数据追加进入该任务；跨周期新建任务 | ✅ |
| 13 | 清洗任务是否长期代表“分析师负责的品类/平台工作区”？ | 清洗任务按品类+平台+月份维护，不是永久无限任务；清洗任务页展示待入清洗队列数量，并支持品类筛选；未来可扩展为多平台/多月份合并任务 | ✅ |
| 14 | 清洗任务周期第一版按什么口径？ | 第一版按月：同一品类+平台+月份已有任务时追加数据，不同月份创建不同任务；未来可扩展支持多月合并 | ✅ |
| 15 | 分发规则的规则值中出现 `/`、`、`、`,` 等分隔符时如何理解？ | 视为多个候选值 OR 匹配；例如 `回音壁/Soundbar音响` 表示命中 `回音壁` 或 `Soundbar音响` 任一类目值，同时保留完整值匹配兼容已有类目全称 | ✅ |
| 16 | 分发完成后下载按什么口径和模板导出？ | 在分发页提供独立“分发结果下载”入口，按品类和/或平台筛选后异步创建导出任务，跨各上传文件的最新已完成分发批次串联导出已分发 raw_data；为避免全量大数据导出拖垮服务，至少选择品类或平台；上传模板尽量保持一致，如混入多模板第一版按模板分 Sheet | ✅ |
| 17 | 分发结果下载新增“时间”筛选时，时间口径按哪个字段？ | 按原始数据月份 `raw_data.month`（YYYYMM）筛选；导出条件支持品类、平台、月份任意组合，至少选择一个条件后才能创建导出任务 | ✅ |
| 18 | 分发结果下载的月份筛选控件采用什么交互？ | 不使用数字输入框，改为月份选择/输入体验，避免聚焦默认显示 `100001` 这类非业务月份 | ✅ |
| 19 | 分发结果下载创建任务后是否需要可查看任务列表，以及列表展示哪些信息？ | 需要。创建任务只负责发起导出查询；页面下方增加任务列表，展示任务当时选择的月份、品类、平台、创建时间、状态、进度和下载/重试等操作 | ✅ |
| 20 | 分发结果下载筛选条件无命中数据时，任务应显示为失败还是空结果？ | 已确认：采用创建前拦截。点击创建导出任务时后端先预查筛选条件是否有可导出数据；无数据则不创建任务，直接提示“当前筛选条件无可导出数据，请调整月份、品类或平台” | ✅ |
| 21 | 分发结果下载页导出任务列表是否需要记录下载人？ | 需要，在现有导出任务列表中新增下载人列，展示去重后的登录用户显示名列表；同一导出文件内同一下载人重复下载时不重复加入名单，只刷新最后下载时间 | ✅ |

---
| 17 | 分发结果下载页导出任务是否需要轮询刷新进度？ | 允许前端每 10 秒内部轮询更新任务状态，但轮询过程不展示表格或按钮加载态，避免用户感知列表持续刷新 | ✅ |

## 数据清洗 / 干预规则

| # | 问题 | 结论 / 默认假设 | 状态 |
|---|------|----------------|------|
| 1 | 干预规则是否需要支持“过滤”和“放行”两类动作？ | 支持两个动作：过滤=命中后进入干扰项存档；放行=命中后保留并跳过后续过滤规则 | ✅ |
| 2 | 干预规则的生效范围是什么？ | 按品类生效，category_code 必填；干预主要用于品类清洗 | ✅ |
| 3 | 多条干预规则同时命中时如何裁决？ | 按 priority 从小到大执行，首条命中即决定过滤或放行；同优先级按 id 先后 | ✅ |
| 4 | 复合规则里的多个值如何匹配？ | 同一条件内多值是 OR，不同条件之间是 AND；例如品牌多个值命中任一品牌，且价格满足阈值，且商品名不包含任一排除词 | ✅ |
| 5 | 旧干扰词历史数据是否需要迁移保留？ | 不需要保留历史数据；可以清掉旧干扰词数据，按新清洗干预规则重新录入 | ✅ |
| 6 | 干扰项规则弹窗当前品类规则列表是否支持编辑已有规则？ | 在每条规则操作区新增“修改”按钮，点击后用现有快速新增表单进入编辑模式，回填规则名称、动作、优先级和条件，保存后调用现有 PATCH 接口更新 | ✅ |
| 7 | 干扰项规则里的“参考价格”应按哪个字段判断？ | 已确认：复核详情页展示的是“价格”而非“参考价格”，干扰项规则的价格条件应与页面可见的价格口径对齐；当前 `ref_price` 为空但 `price=229` 导致低价规则不生效，需修正为使用页面展示价格或至少在 `ref_price` 为空时回退到 `price` | ✅ |

---

## 清洗任务列表

| # | 问题 | 结论 / 默认假设 | 状态 |
|---|------|----------------|------|
| 1 | 清洗任务列表如何直观区分「已经清洗完成」和「还有待处理」？ | 在「状态」列现有 Tag 旁并列展示一个绿色「清洗完成」小徽标（`Tag icon=<CheckCircleFilled/> color=success`），判定口径为 `pending_count === 0`；不改动原有状态标签的文案和颜色，只做视觉二次标识 | ✅ |
| 2 | 清洗任务删除后如何展示？ | 已确认：新增「已删除」Tab；删除后任务从默认清洗任务列表移入「已删除」Tab | ✅ |
| 3 | 已删除清洗任务是否支持恢复或继续处理？ | 默认假设：已删除任务只允许预览查看，不支持进入处理、不支持恢复；如需恢复另行补充需求 | ⏳ 待确认 |

---

## 任务复核工作台单条撤销

| # | 问题 | 结论 / 默认假设 | 状态 |
|---|------|----------------|------|
| 1 | 「排除」/「人工确认」/「暂存争议」误操作后如何回到原状态？ | `match_results` 新增 7 个 `prev_*` 快照列，每次人工三态操作前记录当前 status/model_id/matched_by/match_source/dispute_reason/review_note/reviewed_at；`POST /api/match/items/{id}/revert` 一步回滚 | ✅ |
| 2 | 撤销按钮在什么场景下展示？ | 只要当前 status ∈ {confirmed, excluded, disputed} 就展示：有 `prev_*` 快照时精准回到操作前状态；没有快照的老数据（迁移前遗留的误操作）兜底重置为 pending，避免用户看不到按钮 | ✅ |
| 3 | 撤销「人工确认」时是否同步回滚 URL 映射库？ | 不自动回滚：URL 映射库是长期沉淀，若同链接已被其他条依赖回滚会误伤；页面若需修正链接映射，需另走「映射管理」维护 | ✅ |
| 4 | 是否支持多级撤销 / 撤销队列？ | 只支持一级撤销：单条 `prev_*` 快照回滚一次后立即清空，避免快照链无限增长 | ✅ |

---

## 任务复核工作台单条转移

| # | 问题 | 结论 / 默认假设 | 状态 |
|---|------|----------------|------|
| 1 | 分发出错时误分到错误清洗任务的数据，如何补救？ | 复核详情面板新增「转移到其他任务」按钮，替换现有的纯展示状态 Tag；把该条 match_result 从当前任务移到目标任务并按目标品类重新跑匹配 | ✅ |
| 2 | 转移的目标任务范围是否按品类限制？ | 不限制品类：分发本身可能因为商品名判定错误导致品类错误，跨品类转移是必需能力 | ✅ |
| 3 | 目标任务选择器采用什么交互？ | 搜索选择框（Select showSearch）：模糊搜索任务名/品类码/品类名/平台，Option 显示品类+平台+月份+任务名+id；排除当前任务和归档任务 | ✅ |
| 4 | 转移后原任务和目标任务里的记录如何处理？ | 硬转移：把 match_result 的 clean_job_id 直接改为目标任务；状态重置为 pending 并按目标任务品类重新跑匹配（清空 prev_*、model_id、matched_by、match_source、dispute_reason、review_note、reviewed_at、候选表、属性表） | ✅ |
| 5 | 源任务里的记录去向？ | 硬转移后源任务里彻底消失，另存一条 `match_transfer_logs` 审计日志（match_result_id / raw_data_id / from_clean_job_id / to_clean_job_id / operator / transferred_at） | ✅ |
| 6 | 是否支持撤销转移？ | 不支持撤销：转移是明确的人工纠错操作，如需回滚可再执行一次反向转移 | ✅ |
| 7 | 目标任务已存在相同 raw_data 的记录如何处理？ | 严格拒绝：预检时如目标任务已有相同 raw_data_id 的 match_result 直接返回 400，不做覆盖/合并 | ✅ |
| 8 | 转移动作是否需要弹窗确认？ | 需要：Modal 展示当前商品信息 + 目标任务信息 + 说明文案（重跑匹配、状态重置、不可撤销、不回滚 URL 映射库），点确认再调接口 | ✅ |
| 9 | 转移入口在哪些 Tab 生效？ | URL 映射待确认 / 待确认 / 争议复核 / 未识别品牌 / 已匹配 / 已人工确认 / 已排除；干扰过滤 Tab 不参与（数据结构不同） | ✅ |

---

## Agent Tool 模块

| # | 问题 | 结论 / 默认假设 | 状态 |
|---|------|----------------|------|
| 1 | Agent Tool 模块优先采用哪种接入方式？ | 采用后端自建方案：在 backend 内实现 ToolDefinition、ToolRegistry、ToolExecutor，由服务把工具列表交给 agent 调用；新增 tool 新增 Python 文件即可 | ✅ |
| 2 | 是否需要第一版直接接入具体 LLM Provider？ | 默认假设：第一版先做 provider-neutral 的工具定义与注册中心，并预留 Claude/Anthropic tools 适配器，不把业务工具绑定死在单一 Provider SDK 上 | ⏳ 待确认 |
| 3 | Tool 是否允许执行有副作用操作？ | 默认假设：允许，但必须通过 risk_level 标记副作用等级；删除、发布、外部发送等高风险工具默认需要显式确认或由上层策略拦截 | ⏳ 待确认 |

---

## Go 限流组件

| # | 问题 | 结论 / 默认假设 | 状态 |
|---|------|----------------|------|
| 1 | 限流组件采用哪种算法和集成形态？ | 默认假设：先实现可复用 Go 库，采用令牌桶算法，支持阻塞等待、非阻塞尝试和并发安全；不先接入现有 Python/React 平台 | ⏳ 待确认 |
| 2 | 运行时配置是否需要支持热更新或分布式限流？ | 默认假设：第一版只做单进程内存限流，不引入 Redis、数据库或配置热更新；后续如有多实例需求再扩展 | ⏳ 待确认 |

---

## 生产运行 / 数据库连接池

| # | 问题 | 结论 / 默认假设 | 状态 |
|---|------|----------------|------|
| 1 | 登录页出现 `QueuePool limit of size 5 overflow 10 reached` 时，是否由长耗时数据处理请求占满主库连接池导致？ | 默认假设：是。当前后端生产单 worker 使用 SQLAlchemy 默认主库池上限 15，清洗、导入、导出等同步数据处理请求可能长期持有连接，导致登录请求 30 秒内拿不到连接；已先将连接池参数显式化并提高默认容量，仍需结合线上慢查询、进程列表和接口访问日志确认 | ⏳ 待确认 |
| 2 | 生产 MySQL `max_connections` 和应用连接池容量是否需要按峰值并发重新核算？ | 默认假设：需要。后端主库和分析库各自建池，默认调整后单进程理论上限为每库 `pool_size 20 + max_overflow 30`，需确认 MySQL 可承载连接数以及线上是否会增加 uvicorn worker 数 | ⏳ 待确认 |

---

## 任务复核工作台搜索维度

| # | 问题 | 结论 / 默认假设 | 状态 |
|---|------|----------------|------|
| 1 | "入库品牌"具体指哪个字段？ | 指匹配页详情"入库品牌"栏，通过 `MatchResult.model_id` 关联 `ModelRecord.brand_code` | ✅ |
| 2 | 输入英文编码还是中文品牌名？ | 两者都支持：搜索"入库品牌"时对 `ModelRecord.brand_code` 与品牌表 `BrandRecord.brand_name` 任一做 `ilike` 模糊匹配 | ✅ |
| 3 | 下拉框如何放置？ | 复核工作台顶部搜索框左侧独立放一个 `Select`（宽度 130px），搜索框宽度调整为 180px | ✅ |
| 4 | 默认维度与切换维度时的行为？ | 默认"商品名称"；切换维度时清空输入内容并重置页码到第 1 页 | ✅ |
| 5 | 三个维度是否覆盖复核工作台全部 8 个 tab？ | 是；`list_pending`（前 7 个 tab）和 `list_filtered_items`（干扰项 tab）均支持 `search_by` 参数 | ✅ |
| 6 | 干扰项 tab 下如何处理"入库品牌"？ | 干扰项 tab 下拉框仅显示"商品名称""原品牌"两项；切到该 tab 时若当前维度为"入库品牌"则自动回落到"商品名称"并清空搜索 | ✅ |
| 7 | 干扰项 tab 是否保留原来按"命中规则/关键词/原因"搜索的能力？ | 不保留；本次改造后干扰项按 `matched_keyword` / `intervention_rule_name` / `matched_reason` 搜索的能力去掉 | ✅ |

---

## 任务复核工作台导出

| # | 问题 | 结论 / 默认假设 | 状态 |
|---|------|----------------|------|
| 1 | 匹配页"导出"按钮当前遗漏了哪些 tab 数据？ | 现导出仅覆盖 URL 映射待确认 / 待确认 / 未识别品牌 / 已匹配 / 已人工确认；遗漏 **争议复核**、**已排除**、**干扰项过滤** 三个 tab | ✅ |
| 2 | 三类补充数据在导出文件里如何组织 Sheet？ | 各一个总 Sheet：`争议复核`、`已排除`、`干扰项过滤`；不按品类再拆分 | ✅ |
| 3 | 是否一并导出 `is_disabled=1` 的已禁用记录？ | 不导出；已禁用列表在 UI 上是单独入口，不属于底部 8 个 Tab | ✅ |
| 4 | 干扰项过滤 Sheet 是否包含已恢复（`is_recovered=1`）记录？ | 仅导出 `is_recovered=0` 未恢复的记录，避免与其他 Sheet 中已回流的数据重复 | ✅ |
| 5 | 争议复核 / 已排除 Sheet 的列结构？ | 与 `待确认` Sheet 一致，使用基础列（BASE_COLS）；若 `model_id` 存在则填充品牌/型号信息 | ✅ |
| 6 | 干扰项过滤 Sheet 的列结构？ | 在基础列基础上追加 `命中关键词`、`命中规则`、`命中原因` 三列，取自 `filtered_items` 表 | ✅ |
| 7 | URL 映射待确认导出 Sheet 如何命名？ | 已确认：与复核工作台 Tab 文案一致，按品类命名为 `{品类}-URL映射待确认`，不再使用 `{品类}-待审核` | ✅ |

---

## 任务复核工作台详情展示

| # | 问题 | 结论 / 默认假设 | 状态 |
|---|------|----------------|------|
| 1 | 详情处理里的品类属性应展示哪些字段？ | 只展示当前已选择型号的数据；按品牌配置中的必填/选填分组展示；不再展示“品类字段要求”区域 | ✅ |

---

## 品牌管理搜索与修改

| # | 问题 | 结论 / 默认假设 | 状态 |
|---|------|----------------|------|
| 1 | 品牌名称修改接口的测试样例路径写作 `/brands/{brand_code}`，但任务接口和现有路由前缀为 `/api/brands/{brand_code}`，最终对外路径以哪一个为准？ | 默认假设采用现有 API 前缀与任务接口：`PATCH /api/brands/{brand_code}` | ⏳ 待确认 |
| 2 | 修改品牌名称弹窗里的“写法别名”具体指什么？ | 已确认：写法别名是品牌的同一组别名数据；管理入口直接放到“修改品牌名称”弹窗中展示整组写法别名，支持新增/编辑/删除，不再在展开行单独放一套编辑面板 | ✅ |

---

## 匹配结果独立页面

设计文档：[docs/superpowers/specs/2026-07-12-match-results-page-design.md](docs/superpowers/specs/2026-07-12-match-results-page-design.md)

实现：2026-07-12 已完成，路径 `/match-results`；后端接口 `GET /api/match/reviewed`；共享组件 `frontend/src/pages/Match/components/ReselectModal.tsx`。

| # | 问题 | 结论 / 默认假设 | 状态 |
|---|------|----------------|------|
| 1 | 现有匹配页底部「已匹配 / 已确认条目」卡片藏太深，是否抽独立页面？ | 抽为独立顶级页 `/match-results`（菜单名「匹配结果」），挂在"处理工作台"分组下 | ✅ |
| 2 | 独立页视角是绑定单任务还是全局？ | **全局视图**，同时匹配页保留一个「查看本任务匹配结果」按钮，跳转时自动带上 `job_id` 筛选 | ✅ |
| 3 | 筛选维度包含哪些？ | 任务(job_id) + 匹配来源(match_source, 多选) + 价格预警(price_flag) + 关键词(item_name 模糊)；品牌/品类、时间范围一期不做 | ✅ |
| 4 | 「已匹配」和「已确认」怎么呈现？ | 顶部三 Tab：`全部` / `待复核 · 自动匹配` / `已人工确认`，带数字徽标；默认停在「全部」 | ✅ |
| 5 | 页面上支持哪些操作？ | 仅「重新选择」（复用现有 Modal，抽成公共组件）；不做取消确认、排除、批量重选 | ✅ |
| 6 | 权限怎么设？ | 复用 `processing_workbench`，不新增权限键 | ✅ |
| 7 | 匹配确认页底部原卡片保留还是删除？ | **保留**，它挂着量价审核的调整系数编辑能力，跟本次目标不重叠；新页面是新入口，不是替代 | ✅ |
| 8 | 不选任务时如何避免数据量过大？ | 允许"不选任务=全局"，服务端强制分页（`page_size <= 100`）、默认按 `id DESC` 排列 + UI 上小字提示"未选任务时展示全库最新结果" | ✅ |
| 9 | Tab 徽标计数是否随筛选联动？ | 联动。后端 `GET /api/match/reviewed` 返回 `counts: { all, pending_review, confirmed }` 三项，一次响应喂三个 Tab | ✅ |
| 10 | 老接口 `GET /api/match/{clean_job_id}/reviewed` 怎么处理？ | 保留不动。匹配页量价审核卡片继续用；新页面走新接口 `GET /api/match/reviewed`（无 `clean_job_id` 路径参数） | ✅ |
| 11 | 清洗任务详情页的「匹配结果」入口放在截图箭头指示的右侧空白位时，是移动现有按钮还是新增一个重复入口？ | 已确认：移动现有「查看本任务匹配结果」按钮到顶部操作行最右侧，保留跳转到 `/match-results?job_id=<当前任务>` 的行为；不新增重复入口 | ✅ |

---

---

## 分发管理

| # | 问题 | 结论 / 默认假设 | 状态 |
|---|------|----------------|------|
| 1 | “查看明细”的统计接口是否允许点击时重算规则命中数？ | 默认假设：不应在同步详情接口中扫描原始数据重算；优先使用分发时已落库的 `dispatch_items.matched_rule_id` 汇总，或改为后台异步统计后展示 | ⏳ 待确认 |

---

## 成品管理-数据导出

| # | 问题 | 结论 / 默认假设 | 状态 |
|---|------|----------------|------|
| 1 | 数据导出的筛选入口是否继续使用“清洗任务”下拉？ | 已确认：不再按清洗任务选择；改为按月度（可多选）、品类、平台（可多选）导出 | ✅ |
| 2 | “月度”选项从哪里生成？ | 已确认：从已有结果月份生成可选项，避免选择没有结果数据的月份 | ✅ |

---

## 清洗队列性能排查

| # | 问题 | 结论 / 默认假设 | 状态 |
|---|------|----------------|------|
| 1 | 线上/测试库是否已执行 `p27a1b2c3d4e5_clean_page_performance_indexes` 迁移，确保 `dispatch_items(raw_data_id, category_code, id)` 和 `match_results(clean_job_id, match_status)` 索引存在？ | 默认假设：需要在目标环境确认迁移版本和索引实际存在；否则清洗队列首屏聚合和任务列表状态统计会明显变慢 | ⏳ 待确认 |
| 2 | `GET /api/clean/pool/monthly` 在真实库上的执行计划、扫描行数和耗时是多少？ | 默认假设：该接口首屏无筛选条件时会对全部未清洗派发明细做 join、反连接、distinct count 和 group by，是“待入清洗队列”接口慢的主要来源 | ⏳ 待确认 |

---

## 权限管理

| # | 问题 | 结论 / 默认假设 | 状态 |
|---|------|----------------|------|
| 1 | 细粒度权限是否需要同时控制前端菜单可见性、页面按钮/操作可见性以及后端接口访问？ | 默认假设：三者都需要控制；前端用于减少误操作，后端作为最终权限边界 | ⏳ 待确认 |
| 2 | 品类级权限是否以现有品类编码（如门锁、摄像头对应的 category_code）为唯一授权维度？ | 已确认：门锁、摄像头只是示例；实际从系统现有品类中选择，支持单选或多选品类授权 | ✅ |
| 3 | 品牌管理这类无明显品类归属的功能是否只做功能级权限，不做品类限制？ | 默认假设：品牌管理为全局功能，仅按功能点授权 | ⏳ 待确认 |

---

## 清洗任务

| # | 问题 | 结论 / 默认假设 | 状态 |
|---|------|----------------|------|
| 1 | 月度重新清洗命中已归档任务时应该怎么处理？ | 已确认：将归档任务恢复为活跃任务状态（`reviewing`）后继续追加并重新清洗，不再拦截 | ✅ |
| 2 | 待入清洗队列是否只展示未被已删除清洗任务覆盖的数据？ | 已确认：清洗任务里有的、且关联任务不是已删除（`archived`）的，不展示；只有不存在清洗任务或关联任务已删除时才展示 | ✅ |
