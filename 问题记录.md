# 待确认问题记录

> 整理需求方未明确的问题，统一确认后更新。✅ = 已确认，⏳ = 待确认。

---

## 五期：价格审核 + 销量调整

| # | 问题 | 结论 / 默认假设 | 状态 |
|---|------|----------------|------|
| 1 | 价格审核的“过去 6 个月历史均价”按哪个维度计算？ | 按型号维度计算：同一 model_id/model_code 的 published_items.price 历史均价 | ✅ |
| 2 | 销量调整系数与已有修正规则如何叠加？ | 先应用已有修正规则，再乘单条 sales_coefficient，单条人工调整作为最后兜底 | ✅ |
| 3 | 价格异常是否阻断发布？ | 只提示不阻断发布；high/low/no_history 在匹配确认页展示预警，发布仍可继续 | ✅ |
| 4 | 五期采用独立审核页还是集成到现有匹配确认页？ | 采用轻量集成方案：价格预警和销量系数直接放在现有匹配确认页 matched/confirmed 列表中 | ✅ |

---

## 六期：数据看板 + 灵活导出

| # | 问题 | 结论 / 默认假设 | 状态 |
|---|------|----------------|------|
| 1 | 第六期数据看板优先服务哪种使用场景？ | 优先运营分析视角：筛选后看明细聚合表、品牌/型号/平台排行，偏可查可导出 | ✅ |
| 2 | 运营看板主表默认按什么粒度聚合？ | 支持切换聚合粒度：品牌 / 型号 / 平台 / 品类，默认型号 | ✅ |
| 3 | 第六期灵活导出应该导出哪类数据？ | 聚合结果和当前筛选下的发布明细都要支持导出；明细导出支持字段勾选 | ✅ |
| 4 | 看板里的销量指标默认使用哪个口径？ | 原始销量和修正后销量都展示，默认排序使用修正后销量；修正后销量为空时回退原始销量 | ✅ |
| 5 | 销额和均价是否也要做双口径？ | 销量双口径；销额和均价第六期暂不做双口径，继续使用原始销额/原始均价，避免五期只调销量不调销售额导致口径混淆 | ✅ |
| 6 | 第六期是否要实现同比指标？ | 暂不做同比；先把筛选、聚合、排行、导出做好 | ✅ |
| 7 | 第六期看板筛选条件先做哪些？ | 年月、品牌、品类、平台、型号关键词、商品关键词全都要支持 | ✅ |
| 8 | 明细导出的字段勾选是否需要记住上次选择？ | 暂不记忆偏好；导出弹窗默认全选，用户临时取消勾选即可 | ✅ |
| 9 | 第六期采用哪种实现方案？ | 采用方案 1：运营分析表优先、轻图表；先做筛选区、指标卡、可切换聚合表和两类导出 | ✅ |
| 10 | 第六期后端 API 和指标口径如何设计？ | 新增 /api/analytics 独立路由；提供 filters、summary、summary export、detail export；修正后销量用 COALESCE(corrected_sales_qty, sales_qty)，均价用原始销额 / 原始销量 | ✅ |
| 11 | 第六期前端页面交互如何设计？ | 新增 /dashboard 数据看板；筛选后点击查询刷新；聚合粒度可切换；默认按修正后销量倒序；明细导出弹窗默认全选字段 | ✅ |

---

## 历史库匹配

| # | 问题 | 结论 / 默认假设 | 状态 |
|---|------|----------------|------|
| 1 | 历史原数据的 Excel 列结构是否与现有上传 Excel 一致（平台/商品名/价格/数量等）？ | 暂按与现有上传格式相同处理 | ⏳ 待需求方确认 |
| 2 | 历史正确数据库（确认对照表）的 Excel 具体有哪些列？ | 历史均为 Excel 记录，无已有系统；最少包含平台、商品ID、型号 | ⏳ 待需求方确认列结构 |
| 3 | 历史原数据与历史对照表通过什么字段关联？ | **平台 + 商品ID**（已确认） | ✅ |
| 4 | 历史原数据 Excel 是否包含月份字段？格式是什么？ | 暂按包含 `month` 列、格式 `2024-01` 处理 | ⏳ 待需求方确认 |
| 5 | 历史对照表会有多份吗？如有，同一商品在不同批次型号不一致时以哪次为准？ | 当前实现：upsert 以最新导入批次为准；按批次删除时会同步删掉被覆盖过的商品 | ⏳ 待需求方确认是否接受此行为 |
| 6 | 匹配汇总页面是否需要区分「历史库命中」与「文本匹配」的数量统计？ | 当前实现：历史库命中计入 `matched` 汇总桶，`来源` 列可按条查看，但无专项聚合计数 | ⏳ 待需求方确认是否需要新增计数器 |

---

## 原始数据

| # | 问题 | 结论 / 默认假设 | 状态 |
|---|------|----------------|------|
| 1 | 原始数据页价格筛选的区间边界如何处理？ | 按闭区间处理：最低价 `>= price_min`、最高价 `<= price_max`；前端只保留最低价、最高价两个输入框，不做快捷区间选项 | ✅ |
| 2 | 原始数据页价格筛选是否影响统计和导出？ | 列表、统计、导出共用同一套筛选条件处理 | ✅ |
| 3 | 原始数据页月份筛选是否支持多选？ | 支持多月份查询；列表、统计、导出都按所选多个月份过滤 | ✅ |
| 4 | 原始数据页是否保留文件筛选？ | 暂时隐藏「选择文件」筛选项，保留其他筛选条件 | ✅ |

---

## 前端页面布局调整

| # | 问题 | 结论 / 默认假设 | 状态 |
|---|------|----------------|------|
| 1 | 映射管理主筛选区是否保留导入年份、导入月份？ | 先删除主筛选区里的「导入年份」「导入月份」，保留「导入 Excel」按钮；如后续需要导入批次年月，再放入导入弹窗 | ✅ |

---

## 数据上传

| # | 问题 | 结论 / 默认假设 | 状态 |
|---|------|----------------|------|
| 1 | 是否保留上传确认/列映射步骤？ | 保留上传确认步骤，用户仍需确认列映射后再入库；不额外增加前 N 行字段格式预检查 | ✅ |
| 2 | 上传稳定性优先采用哪种方案？ | 采用方案 A：先做持久化上传任务、分阶段进度和断线后继续查看；暂不引入外部任务队列，不在本阶段重写 Excel 流式解析 | ✅ |
| 3 | 上传处理任务列表应该显示哪些任务？ | 仅显示已拿到文件名且仍在处理或失败的任务；已完成任务归入上传历史，不在任务列表长期展示 | ✅ |
| 4 | 上传处理任务是否需要取消、恢复和提速能力？ | 先做取消按钮、自动识别服务重启造成的中断任务、去重/写入批次提速；暂不做继续处理/恢复 | ✅ |
| 5 | 上传历史的上传时间按什么时区展示？ | 后端统一按北京时间字符串返回，前端直接展示，不再按浏览器时区二次解析 | ✅ |
| 6 | 数据上传页是否保留上传月份维度？ | 删除上传月份选择、上传历史月份筛选和历史表格月份列；文件自身月份范围仍保留 | ✅ |
| 7 | 上传历史长文件名如何查看完整内容？ | 上传历史表格支持横向滚动，文件名列加宽并关闭省略，用户可拖动横向滚动查看完整文件名 | ✅ |
| 8 | 上传处理任务失败后是否需要清理入口？ | 失败/已取消的上传处理任务支持删除，运行中和等待中任务需先取消或结束后再删除 | ✅ |

---

## 数据分发

| # | 问题 | 结论 / 默认假设 | 状态 |
|---|------|----------------|------|
| 1 | 分发明细中“未识别”数据是否需要可查看具体原始行？ | 在分发明细中显示“未识别 X 条”，点击打开分页明细弹窗查看未命中 raw_data 原始商品行 | ✅ |
| 2 | 分发规则命中统计是否需要支持直接编辑对应规则？ | 支持点击规则打开编辑面板并保存规则；保存后只更新规则配置，不自动改已完成批次，提示需要重新分发后生效 | ✅ |
| 3 | 分发结果增强采用哪种页面方案？ | 采用方案 A：增强现有“查看明细”弹窗，分区展示汇总、品类统计、规则命中统计，并通过二级弹窗查看未识别明细 | ✅ |
| 4 | 分发规则筛选后总数量显示在哪里？ | 放在规则表格下方左侧，显示当前筛选结果总数 | ✅ |

---
