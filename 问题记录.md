# 待确认问题记录

> 整理需求方未明确的问题，统一确认后更新。✅ = 已确认，⏳ = 待确认。

---

## 服务器迁移

| # | 问题 | 结论 / 默认假设 | 状态 |
|---|------|----------------|------|
| 1 | 旧服务器数据迁移到新服务器时是否允许短暂停机？ | 允许短暂停机，优先采用最终停机导出数据库并同步持久化文件，保证数据一致性 | ✅ |

---

## 五期：价格审核 + 销量调整

| # | 问题 | 结论 / 默认假设 | 状态 |
|---|------|----------------|------|
| 1 | 价格审核的“过去 6 个月历史均价”按哪个维度计算？ | 按型号维度计算：同一 model_id/model_code 的 published_items.price 历史均价 | ✅ |
| 2 | 销量调整系数与已有修正规则如何叠加？ | 先应用已有修正规则，再乘单条 sales_coefficient，单条人工调整作为最后兜底 | ✅ |
| 3 | 价格异常是否阻断发布？ | 只提示不阻断发布；high/low/no_history 在匹配确认页展示预警，发布仍可继续 | ✅ |
| 4 | 五期采用独立审核页还是集成到现有匹配确认页？ | 采用轻量集成方案：价格预警和销量系数直接放在现有匹配确认页 matched/confirmed 列表中 | ✅ |

---

## 六期：数据看板 + 灵活导出

| # | 问题 | 结论 / 默认假设 | 状态 |
|---|------|----------------|------|
| 1 | 第六期数据看板优先服务哪种使用场景？ | 优先运营分析视角：筛选后看明细聚合表、品牌/型号/平台排行，偏可查可导出 | ✅ |
| 2 | 运营看板主表默认按什么粒度聚合？ | 支持切换聚合粒度：品牌 / 型号 / 平台 / 品类，默认型号 | ✅ |
| 3 | 第六期灵活导出应该导出哪类数据？ | 聚合结果和当前筛选下的发布明细都要支持导出；明细导出支持字段勾选 | ✅ |
| 4 | 看板里的销量指标默认使用哪个口径？ | 原始销量和修正后销量都展示，默认排序使用修正后销量；修正后销量为空时回退原始销量 | ✅ |
| 5 | 销额和均价是否也要做双口径？ | 销量双口径；销额和均价第六期暂不做双口径，继续使用原始销额/原始均价，避免五期只调销量不调销售额导致口径混淆 | ✅ |
| 6 | 第六期是否要实现同比指标？ | 暂不做同比；先把筛选、聚合、排行、导出做好 | ✅ |
| 7 | 第六期看板筛选条件先做哪些？ | 年月、品牌、品类、平台、型号关键词、商品关键词全都要支持 | ✅ |
| 8 | 明细导出的字段勾选是否需要记住上次选择？ | 暂不记忆偏好；导出弹窗默认全选，用户临时取消勾选即可 | ✅ |
| 9 | 第六期采用哪种实现方案？ | 采用方案 1：运营分析表优先、轻图表；先做筛选区、指标卡、可切换聚合表和两类导出 | ✅ |
| 10 | 第六期后端 API 和指标口径如何设计？ | 新增 /api/analytics 独立路由；提供 filters、summary、summary export、detail export；修正后销量用 COALESCE(corrected_sales_qty, sales_qty)，均价用原始销额 / 原始销量 | ✅ |
| 11 | 第六期前端页面交互如何设计？ | 新增 /dashboard 数据看板；筛选后点击查询刷新；聚合粒度可切换；默认按修正后销量倒序；明细导出弹窗默认全选字段 | ✅ |

---

## 历史库匹配

| # | 问题 | 结论 / 默认假设 | 状态 |
|---|------|----------------|------|
| 1 | 历史原数据的 Excel 列结构是否与现有上传 Excel 一致（平台/商品名/价格/数量等）？ | 历史库按“历史原始明细 + 确认型号结果在同一张 Excel 表”处理，每行视为一条线下已确认的工作台结果 | ✅ |
| 2 | 历史正确数据库（确认对照表）的 Excel 具体有哪些列？ | 当前样例列：年、月、周、报告类型、渠道、商场、品类、品牌、型号、品类码、品牌码、型号码、标题、销额、销量、单价、网址；其中「型号」视为确认型号，但历史线下不完全规范，可能是型号编码或型号名称，导入时先按 model_code 匹配，再按 model_name 唯一命中匹配 | ✅ |
| 3 | 历史原数据与历史对照表通过什么字段关联？ | 第一版优先按 URL 解析出的平台+商品ID、平台+网址原文、平台+商场+标题归一化完全一致做高置信匹配；样例无显式商品ID列 | ✅ |
| 4 | 历史原数据 Excel 是否包含月份字段？格式是什么？ | 包含独立「年」「月」「周」列；导入时组合为 month=YYYY-MM，并保留 week | ✅ |
| 5 | 历史对照表会有多份吗？如有，同一商品在不同批次型号不一致时以哪次为准？ | 旧历史库数据当前无有效存量，可直接废弃不迁移；新历史库按“历史确认结果导入”重建，重复判定需包含数据周期（如 year/month/week 或 month），同一商品不同月份可同时存在，同一周期内重复高置信匹配键以最新导入批次为准 | ✅ |
| 6 | 匹配汇总页面是否需要区分「历史库命中」与「文本匹配」的数量统计？ | 当前实现：历史库命中计入 `matched` 汇总桶，`来源` 列可按条查看，但无专项聚合计数 | ⏳ 待需求方确认是否需要新增计数器 |
| 7 | 历史库匹配新数据时是否允许商品名称模糊相似匹配？ | 第一版只做高置信精确匹配：platform+item_id、URL 解析 item_id、platform+item_url、platform+shop_name+item_name 归一化完全一致；暂不做模糊相似度 | ✅ |
| 8 | 历史库是否允许型号为空的记录导入？ | 允许。型号为空表示历史库里已有商品 URL/品牌等商品线索但型号尚未补全，后续可继续补充；这类记录仍属于历史库，链接是关键匹配依据 | ✅ |
| 9 | 历史库是否需要兼容线下历史数据库的多种旧表头格式？ | 需要。后续历史库导入应支持笔记本、路由器、门锁等历史明细库直接上传，通过列别名和明细 sheet 自动识别转换为标准历史库列 | ✅ |
| 10 | 历史库兼容旧格式导入时是否需要先向用户反馈映射结果？ | 需要。导入流程应类似原始数据上传：先解析 sheet/列映射/年月品类推断并展示预览，用户确认后再正式入库 | ✅ |
| 11 | 历史库 Excel 中的品类属性列如何处理？ | 属性列属于成品字段定义/成品属性管理处理的型号属性体系，不作为历史库映射表的标准字段；导入历史库时只沉淀商品历史明细和确认型号，属性可复用现有成品属性管理导入能力一次性处理 | ✅ |
| 12 | 历史库导入时型号文本找不到现有型号库怎么办？ | 自动创建型号记录；属性仍由产品字段定义/产品属性管理后续维护 | ✅ |
| 13 | 导入三份历史库前是否需要先补齐产品字段定义和产品属性管理？ | 需要。三份历史库属于一次性历史初始化：先从 Excel 中提取各品类属性列和型号属性值，一次性填入产品字段定义/产品属性管理；后续维护主要在平台页面操作，不需要为历史属性导入单独做长期入口 | ✅ |
| 14 | 历史属性初始化遇到品牌或型号为空/为 `-` 的记录如何处理？ | 品牌有效但型号为空或 `-` 时允许保留为待补型号记录，后续再补型号；品牌和型号都为空或 `-` 时不可用，不写入成品属性管理 | ✅ |
| 15 | 匹配确认页是否展示未补规格属性？ | 暂不在匹配确认页待处理区展示未补属性；规格属性缺失不参与当前匹配分流 | ✅ |
| 16 | URL 映射库或历史库链接命中的结果如何命名？ | 统一展示为“精准匹配”，表示商品链接在 URL 映射库或历史库已有命中 | ✅ |
| 17 | 匹配来源是否直接展示 S1/S2/S3/S4 等内部阶段名？ | 不展示内部阶段名，改为业务可读来源文案，如 URL映射命中、历史库命中、品牌字段匹配等 | ✅ |
| 18 | 品牌码或型号码为 `-` / `--` / 空的型号能否作为自动匹配结果？ | 不能作为有效自动匹配结果；页面展示时也不把 `[-] -` 视作有效型号 | ✅ |
| 18.1 | 匹配确认页哪些待处理列表需要展示商品链接？ | 待确认、未识别品牌、URL映射待确认都需要展示商品链接，方便分析师打开商品判断归属 | ✅ |
| 19 | 历史库数据是否需要进入分析库？ | 需要。历史库不仅是匹配参考，也是历史分析数据来源；首次录入平台前的历史数据需要能进入分析库，否则平台缺少既有经营数据 | ✅ |
| 20 | 新数据人工确认后是否沉淀 URL 映射库？ | 需要。URL 映射库是长期沉淀的纯粹匹配库，不只是临时缓存；新数据确认型号后应写入 URL 映射库，供后续同链接精准匹配 | ✅ |
| 21 | 历史库和 URL 映射库的职责如何区分？ | 历史库承担历史数据导入和历史发布，允许信息不完整；URL 映射库承担持续沉淀的链接级匹配关系，尽量保持结构纯粹、面向后续自动匹配 | ✅ |

---

## 待处理工作台

| # | 问题 | 结论 / 默认假设 | 状态 |
|---|------|----------------|------|
| 1 | 有争议、未匹配、URL待确认、未识别品牌数据应如何给分析师处理？ | 第一期按“任务内处理”设计，升级现有匹配确认页为左侧队列 + 右侧详情处理面板，不做个人队列 | ✅ |
| 2 | 待处理工作台需要哪些处理动作？ | 支持确认当前候选、选择其他型号确认、排除、暂存争议；确认后沉淀 URL 映射库，排除/争议不发布且不写 URL 映射库 | ✅ |
| 3 | 是否需要新增争议状态？ | 新增 `disputed` 状态表示暂存争议；发布只发布 `url_matched` / `matched` / `confirmed` | ✅ |
| 4 | 是否需要记录操作人？ | 第一期先记录 `reviewed_at`、`review_note`、`dispute_reason`；操作人/负责人留待个人队列阶段再补 | ✅ |
| 5 | 详情处理里“相同 case”批量确认按什么口径？ | 按商品名称归一化后完全一致判断；执行前必须预览命中条数和受影响记录，避免误批量 | ✅ |
| 6 | 清洗任务详情增强采用什么整体方向？ | 在现有左侧队列 + 右侧详情处理工作台中增强批量确认、属性提示、干扰项规则浮窗和重清洗闭环，不新拆独立页面 | ✅ |
| 7 | 新增干扰项规则后重清洗是否会让已人工确认 URL 的数据重新回到待处理？ | 不应重复让分析师处理；已确认写入 URL 映射库后，重清洗/重匹配会优先通过 URL 映射命中，回到可发布状态而不是待处理 | ✅ |
| 8 | 右侧复核工作台是否只服务待处理/争议数据？ | 不应只看待处理；右侧详情工作台应覆盖待处理、争议、已匹配、已确认，并补充干扰项过滤记录 Tab，方便确认后继续查看属性和完整复核链路 | ✅ |

---

## 原始数据

| # | 问题 | 结论 / 默认假设 | 状态 |
|---|------|----------------|------|
| 1 | 原始数据页价格筛选的区间边界如何处理？ | 按闭区间处理：最低价 `>= price_min`、最高价 `<= price_max`；前端只保留最低价、最高价两个输入框，不做快捷区间选项 | ✅ |
| 2 | 原始数据页价格筛选是否影响统计和导出？ | 列表、统计、导出共用同一套筛选条件处理 | ✅ |
| 3 | 原始数据页月份筛选是否支持多选？ | 支持多月份查询；列表、统计、导出都按所选多个月份过滤 | ✅ |
| 4 | 原始数据页是否保留文件筛选？ | 暂时隐藏「选择文件」筛选项，保留其他筛选条件 | ✅ |

---

## 查询工作台

| # | 问题 | 结论 / 默认假设 | 状态 |
|---|------|----------------|------|
| 1 | 查询工作台顶部筛选项保留哪些？ | 筛选项按年度、月度、品类、平台、品牌、型号、网址展示，并保留「搜索宝贝名称」 | ✅ |
| 2 | 查询工作台结果表格列如何展示？ | 列顺序为序号、年度、月度、品类、平台、商品名称、网址、图片地址、原品牌、型号、型号别名、判断类型、销量、平均价格、操作人、操作时间 | ✅ |
| 3 | 查询工作台“操作人”取哪个业务动作的人？ | 暂无真实落库字段，当前先展示占位 `-`；待确认后可补为匹配确认人、发布人或最后编辑人 | ⏳ 待确认 |

---

## 前端页面布局调整

| # | 问题 | 结论 / 默认假设 | 状态 |
|---|------|----------------|------|
| 1 | 映射管理主筛选区是否保留导入年份、导入月份？ | 先删除主筛选区里的「导入年份」「导入月份」，保留「导入 Excel」按钮；如后续需要导入批次年月，再放入导入弹窗 | ✅ |
| 2 | 产品字段定义和产品属性管理上传前是否提供下载模板？ | 采用后端下载接口：产品字段定义下载后端包内模板「洛图科技—产品段属性说明-模板.xlsx」，产品属性管理动态生成「型号」「型号规格」两 Sheet 模板；前端在导入按钮旁提供下载模板按钮，并打通当前导入流程以读取「型号规格」Sheet，下载模板上传时自动套用内置字段映射 | ✅ |
| 3 | 产品字段定义模板 sheet 名如何识别品类？ | sheet 名可以是已有品类码或品类名称；导入时统一匹配为品类码，匹配不到则报错提示，不允许把未知 sheet 名直接写入品类码 | ✅ |
| 4 | 产品字段定义页品类筛选是否支持输入搜索？ | 支持；筛选下拉可按品类码或品类名称输入搜索 | ✅ |

---

## 产品字段定义 / 产品属性管理

| # | 问题 | 结论 / 默认假设 | 状态 |
|---|------|----------------|------|
| 1 | “价格段”属性是否允许人工录入任意值？ | 默认不允许人工任意录入；价格段作为特殊系统属性，由品类配置分段规则后按商品价格自动计算，例如 `<100`、`100~599`、`600~999`、`>1000` | ⏳ 待确认 |

---

## 用户管理 / 权限管理

| # | 问题 | 结论 / 默认假设 | 状态 |
|---|------|----------------|------|
| 1 | 用户权限是否可以顺手调整现有菜单目录归属？ | 不允许调整现有目录归属；权限必须严格按当前已确定目录包住：数据管理、处理工作台、成品管理，仅允许新增用户管理目录 | ✅ |

---

## 全局时间展示

| # | 问题 | 结论 / 默认假设 | 状态 |
|---|------|----------------|------|
| 1 | 平台内 created_at / updated_at / uploaded_at / finished_at 等用户可见时间按什么时区展示？ | 后端统一按北京时间字符串返回，前端直接展示或截取日期部分，不再用浏览器时区二次解析 | ✅ |

---

## 数据上传

| # | 问题 | 结论 / 默认假设 | 状态 |
|---|------|----------------|------|
| 1 | 是否保留上传确认/列映射步骤？ | 保留上传确认步骤，用户仍需确认列映射后再入库；不额外增加前 N 行字段格式预检查 | ✅ |
| 2 | 上传稳定性优先采用哪种方案？ | 采用方案 A：先做持久化上传任务、分阶段进度和断线后继续查看；暂不引入外部任务队列，不在本阶段重写 Excel 流式解析 | ✅ |
| 3 | 上传处理任务列表应该显示哪些任务？ | 仅显示已拿到文件名且仍在处理或失败的任务；已完成任务归入上传历史，不在任务列表长期展示 | ✅ |
| 4 | 上传处理任务是否需要取消、恢复和提速能力？ | 先做取消按钮、自动识别服务重启造成的中断任务、去重/写入批次提速；暂不做继续处理/恢复 | ✅ |
| 5 | 上传历史的上传时间按什么时区展示？ | 后端统一按北京时间字符串返回，前端直接展示，不再按浏览器时区二次解析 | ✅ |
| 6 | 数据上传页是否保留上传月份维度？ | 删除上传月份选择、上传历史月份筛选和历史表格月份列；文件自身月份范围仍保留 | ✅ |
| 7 | 上传历史长文件名如何查看完整内容？ | 上传历史表格支持横向滚动，文件名列加宽并关闭省略，用户可拖动横向滚动查看完整文件名 | ✅ |
| 8 | 上传处理任务失败后是否需要清理入口？ | 失败/已取消的上传处理任务支持删除，运行中和等待中任务需先取消或结束后再删除 | ✅ |

---

## 数据分发

| # | 问题 | 结论 / 默认假设 | 状态 |
|---|------|----------------|------|
| 1 | 分发明细中“未识别”数据是否需要可查看具体原始行？ | 在分发明细中显示“未识别 X 条”，点击打开分页明细弹窗查看未命中 raw_data 原始商品行 | ✅ |
| 2 | 分发规则命中统计是否需要支持直接编辑对应规则？ | 支持点击规则打开编辑面板并保存规则；保存后只更新规则配置，不自动改已完成批次，提示需要重新分发后生效 | ✅ |
| 3 | 分发结果增强采用哪种页面方案？ | 采用方案 A：增强现有“查看明细”弹窗，分区展示汇总、品类统计、规则命中统计，并通过二级弹窗查看未识别明细 | ✅ |
| 4 | 分发规则筛选后总数量显示在哪里？ | 放在规则表格下方左侧，显示当前筛选结果总数 | ✅ |
| 5 | 原始数据命中多条分发规则时如何处理？ | 分发不是互斥分类；只要命中规则就应分发到对应类目，同一条 raw_data 可以进入多个类目 | ✅ |
| 6 | 同一条 raw_data 命中同一类目的多条分发规则时是否去重？ | 同一 batch + raw_data + category_code 只保留一条，matched_rule_id 记录优先级最高的命中规则 | ✅ |
| 7 | 分发页追加清洗入口时，“全量清洗入库”按什么粒度执行？ | 采用“按类目逐个清洗”的方案：全量入口对该分发批次下每个类目分别创建清洗任务，避免把不同类目混在一个清洗上下文里 | ✅ |
| 8 | 单个类目清洗入口放在哪里？ | 原方案放在分发明细弹窗的类目统计表操作列；现反馈该入口不符合分析师工作流，需调整为分发结束后由分析师在清洗任务页按品类/平台维护任务 | ⏳ 待重新确认 |
| 9 | 分发后的清洗逻辑口径是什么？ | 清洗不是重新分发，而是在当前类目范围内根据干预库排除不符合类目需求的数据，并做必要的标准化处理 | ✅ |
| 10 | 分发后生成的清洗任务如何让分析师识别自己负责的内容？ | 清洗任务需要明显展示来源分发批次、品类/类目、负责人或可归属信息；否则多个分析师无法区分哪些任务属于自己负责的品类 | ✅ |
| 11 | 分发页是否应该承担创建清洗任务？ | 不承担：分发完成后只进入品类/平台待处理池；分析师在清洗任务页按自己负责的品类/平台创建或更新任务 | ✅ |
| 12 | 同一品类/平台已有清洗任务时，新分发数据如何进入任务？ | 采用按周期增量更新：同一品类+平台+月份/周期已有任务时，新分发数据追加进入该任务；跨周期新建任务 | ✅ |
| 13 | 清洗任务是否长期代表“分析师负责的品类/平台工作区”？ | 清洗任务按品类+平台+月份维护，不是永久无限任务；清洗任务页展示待入清洗队列数量，并支持品类筛选；未来可扩展为多平台/多月份合并任务 | ✅ |
| 14 | 清洗任务周期第一版按什么口径？ | 第一版按月：同一品类+平台+月份已有任务时追加数据，不同月份创建不同任务；未来可扩展支持多月合并 | ✅ |
| 15 | 分发规则的规则值中出现 `/`、`、`、`,` 等分隔符时如何理解？ | 视为多个候选值 OR 匹配；例如 `回音壁/Soundbar音响` 表示命中 `回音壁` 或 `Soundbar音响` 任一类目值，同时保留完整值匹配兼容已有类目全称 | ✅ |
| 16 | 分发完成后下载按什么口径和模板导出？ | 按品类+平台下载已分发 raw_data；本质是多个上传文件命中数据串联导出。上传模板尽量保持一致；如同一品类+平台混入多模板，第一版按模板分组导出，避免列结构混乱 | ✅ |

---

## 数据清洗 / 干预规则

| # | 问题 | 结论 / 默认假设 | 状态 |
|---|------|----------------|------|
| 1 | 干预规则是否需要支持“过滤”和“放行”两类动作？ | 支持两个动作：过滤=命中后进入干扰项存档；放行=命中后保留并跳过后续过滤规则 | ✅ |
| 2 | 干预规则的生效范围是什么？ | 按品类生效，category_code 必填；干预主要用于品类清洗 | ✅ |
| 3 | 多条干预规则同时命中时如何裁决？ | 按 priority 从小到大执行，首条命中即决定过滤或放行；同优先级按 id 先后 | ✅ |
| 4 | 复合规则里的多个值如何匹配？ | 同一条件内多值是 OR，不同条件之间是 AND；例如品牌多个值命中任一品牌，且价格满足阈值，且商品名不包含任一排除词 | ✅ |
| 5 | 旧干扰词历史数据是否需要迁移保留？ | 不需要保留历史数据；可以清掉旧干扰词数据，按新清洗干预规则重新录入 | ✅ |

---
