# 洛图数据处理平台 — Claude 工作规范

## 问题记录规则

在讨论需求、方案设计或实现过程中，凡遇到以下情况，**必须**同步更新项目根目录的 `问题记录.md`：

1. **需求方尚未明确的信息**：用 ⏳ 标记，注明"默认假设"（开发暂用）
2. **用户在对话中给出的确认答复**：将对应条目状态改为 ✅，补充确认结论
3. **新发现的待确认问题**：追加到对应功能模块下

### 格式规范

```markdown
## <功能模块名>

| # | 问题 | 结论 / 默认假设 | 状态 |
|---|------|----------------|------|
| N | 问题描述 | 暂定方案或已确认结论 | ⏳ 待确认 / ✅ |
```

> 目的：让产品负责人可以随时打开 `问题记录.md`，一次性拿着所有问题去找需求方确认。

## Git 规范

- **禁止使用 `git add -f`**：`.gitignore` 中列出的路径/文件不允许强制加入版本库，即使是文档、设计稿等非代码文件也不例外。如果某个文件需要被追踪，应先修改 `.gitignore` 并征得用户同意，而不是绕过它。

## 部署规范

- **禁止直接在服务器上修改业务代码**：服务器只用于部署和运行，不作为开发编辑环境。所有代码改动必须先在本地完成、测试、提交到 Git，并推送到远端后，再由服务器拉取已提交代码执行部署。
- **服务器临时修复必须回流 Git**：如遇紧急情况确需在服务器临时排查或改动，必须先征得用户明确同意；事后必须把最终代码改动同步回本地仓库并提交，避免线上代码与 Git 历史分叉。
